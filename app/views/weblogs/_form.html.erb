<%= form_for(@weblog) do |f| %>
  <% if @weblog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weblog.errors.count, "error") %> prohibited this weblog from being saved:</h2>

      <ul>
      <% @weblog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="display:none;">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id, value:session[:login]  %>
  </div>

  <div class="field" style="display:none;">
    <%= f.label :profile_id %><br>
    <%= f.number_field :profile_id, value:@weblog_relation_profile.id  %>
  </div>

  <div class="field">
    <%= f.label :blog_name %><br>
    <%= f.text_field :blog_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
